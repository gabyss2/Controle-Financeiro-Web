
import React from "react"; import { View, Text } from "react-native"; import { Screen, Button } from "../../components/UI"; import { useRouter } from "expo-router";
export default function Home() { const router = useRouter(); return (<Screen title="Controle Financeiro"><View style={{ gap: 8 }}><Text style={{ fontSize: 16 }}>Em breve: Saldo do mês, Receitas, Despesas, Gráficos e Atalhos.</Text><View style={{ flexDirection: "row", gap: 8, flexWrap: "wrap" }}><Button label="Contas Fixas" onPress={() => router.push("/renda")} variant="ghost" /><Button label="Cartões" onPress={() => router.push("/cartoes")} variant="ghost" /><Button label="Débito/PIX" onPress={() => router.push("/gastos")} variant="ghost" /><Button label="Reserva" onPress={() => router.push("/reserva")} variant="ghost" /></View></View></Screen>); }


import React from "react"; import { View, Text, Pressable, StyleSheet } from "react-native";
export const Screen: React.FC<{ title?: string; children: React.ReactNode }> = ({ title, children }) => (<View style={styles.container}>{title ? <Text style={styles.title}>{title}</Text> : null}{children}</View>);
export const Button: React.FC<{ label: string; onPress: () => void; variant?: "primary" | "ghost" }> = ({ label, onPress, variant = "primary" }) => (<Pressable onPress={onPress} style={[styles.btn, variant === "ghost" && styles.btnGhost]}><Text style={[styles.btnText, variant === "ghost" && styles.btnGhostText]}>{label}</Text></Pressable>);
export const Chip: React.FC<{ label: string; active?: boolean; onPress: () => void }> = ({ label, active, onPress }) => (<Pressable onPress={onPress} style={[styles.chip, active && styles.chipActive]}><Text style={[styles.chipText, active && styles.chipTextActive]}>{label}</Text></Pressable>);
const styles = StyleSheet.create({ container: { flex: 1, backgroundColor: "#fff", padding: 20, gap: 16 }, title: { fontSize: 22, fontWeight: "700" }, btn: { backgroundColor: "#1f7a8c", paddingVertical: 12, paddingHorizontal: 16, borderRadius: 10, alignItems: "center" }, btnText: { color: "#fff", fontWeight: "700" }, btnGhost: { backgroundColor: "#e8f1f2" }, btnGhostText: { color: "#1f7a8c" }, chip: { borderWidth: 1, borderColor: "#1f7a8c", paddingVertical: 6, paddingHorizontal: 10, borderRadius: 20, marginRight: 8 }, chipActive: { backgroundColor: "#1f7a8c" }, chipText: { color: "#1f7a8c" }, chipTextActive: { color: "#fff" } });
